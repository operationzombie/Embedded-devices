---
language: c
before_install:
  - sudo apt-get update -qq
install:
  - sudo apt-get install -qq gcc-avr binutils-avr avr-libc

notifications:
  email:
  - paddygarmin@gmail.com
  on_success: always

script: make
